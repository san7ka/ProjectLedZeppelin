version: '3.8'

services:
  web:
    build: .
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
    container_name: text-quest-game
    volumes:
      - ./logs:/usr/local/tomcat/logs
